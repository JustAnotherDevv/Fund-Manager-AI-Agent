{
  "name": "ai-fund-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for AI-powered hedge fund platform",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "create-fund": "hardhat run scripts/create-fund.js --network localhost",
    "interact": "hardhat run scripts/interact.js --network localhost"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.22.19"
  }
}
